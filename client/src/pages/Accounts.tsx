import { useEffect,useState } from "react"; import { Link } from "react-router-dom"; import { api } from "../lib/api";
export default function Accounts(){const [d,setD]=useState<any>({items:[]});const [q,setQ]=useState("");const [f,setF]=useState("all");useEffect(()=>{api<any>(`/accounts?search=${encodeURIComponent(q)}&filter=${f}`).then(setD)},[q,f]);return <><div className="flex justify-between mb-6"><div><h1 className="text-2xl font-bold">Accounts</h1><p className="text-sm text-slate-500 mt-1">Company facts, intelligence and qualification in one view.</p></div><div className="flex gap-3"><select value={f} onChange={e=>setF(e.target.value)} className="border bg-white rounded-lg px-4 text-sm font-medium text-slate-700 outline-none"><option value="all">All Accounts</option><option value="new">New (Unanalyzed)</option><option value="analyzed">Analyzed Only</option></select><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search companies…" className="border bg-white rounded-lg px-4 py-2 w-72 text-sm outline-none"/></div></div><div className="bg-white border rounded-xl overflow-hidden"><table className="w-full text-sm"><thead className="bg-slate-50 text-slate-500"><tr>{["Company","Industry","Location","Employees","ICP","Latest Signal"].map(x=><th className="text-left p-4 font-medium" key={x}>{x}</th>)}</tr></thead><tbody>{d.items.map((a:any)=><tr className="border-t hover:bg-slate-50" key={a.id}><td className="p-4"><Link className="font-semibold hover:underline" to={`/accounts/${a.id}`}>{a.companyName}</Link><div className="text-xs text-slate-400">{a.website}</div></td><td className="p-4">{a.industry??"—"}</td><td className="p-4">{[a.city,a.country].filter(Boolean).join(", ")||"—"}</td><td className="p-4">{a.employeeCount??"—"}</td><td className="p-4">{a.assessments?.[0]?.overallScore ? <span className="font-bold text-blue-700">{a.assessments[0].overallScore}</span> : <span className="px-2 py-1 bg-amber-100 text-amber-700 rounded text-xs font-bold">NEW</span>}</td><td className="p-4 max-w-xs truncate">{a.signals?.[0]?.title??"—"}</td></tr>)}</tbody></table></div></>}
