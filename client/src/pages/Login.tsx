import { useState } from "react"; import { useNavigate } from "react-router-dom"; import { api } from "../lib/api";
export default function Login(){const [email,setEmail]=useState("admin@example.com"),[password,setPassword]=useState("Admin123!"),[error,setError]=useState("");const nav=useNavigate();return <div className="min-h-screen flex items-center justify-center bg-slate-100"><form className="w-96 bg-white border rounded-2xl p-8 shadow-sm" onSubmit={async e=>{e.preventDefault();try{const d=await api<any>("/auth/login",{method:"POST",body:JSON.stringify({email,password})});localStorage.setItem("token",d.token);nav("/")}catch(e:any){setError(e.message)}}}><h1 className="text-xl font-bold">Market Intelligence</h1><p className="text-sm text-slate-500 mt-1 mb-6">Sign in to your workspace</p><input className="w-full border rounded-lg p-3 mb-3" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input className="w-full border rounded-lg p-3 mb-3" value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="Password"/>{error&&<div className="text-sm text-red-600 mb-3">{error}</div>}<button className="w-full bg-gradient-to-r from-theme-peach to-theme-blue text-slate-900 shadow-sm rounded-lg py-3 font-medium">Sign in</button><div className="text-xs text-slate-400 mt-4">Demo: admin@example.com / Admin123!</div></form></div>}
