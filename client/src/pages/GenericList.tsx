import { useEffect,useState } from "react"; import { api } from "../lib/api";
export default function GenericList({title,endpoint,columns}:{title:string;endpoint:string;columns:string[]}){const [items,setItems]=useState<any[]>([]);useEffect(()=>{api<any>(endpoint).then(d=>setItems(d.items??[]))},[endpoint]);return <><h1 className="text-2xl font-bold mb-6">{title}</h1><div className="bg-white border rounded-xl overflow-hidden"><table className="w-full text-sm"><thead className="bg-slate-50"><tr>{columns.map(c=><th className="text-left p-4 font-medium text-slate-500" key={c}>{c}</th>)}</tr></thead><tbody>{items.map((x,i)=><tr className="border-t" key={x.id??i}>{columns.map(c=><td className="p-4" key={c}>{String(x[c.replace(/\s+/g,"").replace(/^./,s=>s.toLowerCase())]??x[c.toLowerCase()]??"—")}</td>)}</tr>)}</tbody></table></div></>}
